# MCP registry - integrity report

Generated by `audit.py prep` from `mcp-latest.jsonl`.

## Shape

- **25125** records
- fields audited: text=`description`, answer=`name`, group=`_status`
- **2** distinct `_status` values (12562.5 items each on average)
- largest accounts for **99.0%** of all records

## Findings

- malformed JSON lines: **0**
- schema issues: **101383**
- degenerate records: **0**
- exact duplicate text groups: **418** (904 records, 3.6%)
- exact duplicate answer groups: **6** (12 records)
- near-duplicate clusters at Jaccard >= 0.7: **487** (1252 records, 4.98%)
  - **0** identical across every evaluated field - 0 records, **0.0%** - genuinely redundant
  - **487** share a text stem but differ elsewhere - deliberate construction, not a defect

## Method

Similarity: 3-word shingles, Jaccard, threshold 0.7. Search: **minhash-lsh** over 126601 candidate pairs.

LSH generates candidates only; every reported pair is verified with the true Jaccard score, so similarity values mean the same thing as under exact search. Recall is not guaranteed - a small number of genuine near-duplicates may be missed, which makes these counts a lower bound.

Rerun `audit.py` to reproduce every number above.
