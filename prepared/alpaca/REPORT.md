# Alpaca - integrity report

Generated by `audit.py prep` from `alpaca.jsonl`.

## Shape

- **52002** records
- fields audited: text=`instruction`, answer=`output`, group=`None`

## Findings

- malformed JSON lines: **0**
- schema issues: **0**
- degenerate records: **28**
- exact duplicate text groups: **0** (0 records, 0.0%)
- exact duplicate answer groups: **383** (1603 records)
- near-duplicate clusters at Jaccard >= 0.7: **0** (0 records, 0.0%)
  - **0** identical across every evaluated field - 0 records, **0.0%** - genuinely redundant
  - **0** share a text stem but differ elsewhere - deliberate construction, not a defect

## Method

Similarity: 3-word shingles, Jaccard, threshold 0.7. Search: **minhash-lsh** over 30779 candidate pairs.

LSH generates candidates only; every reported pair is verified with the true Jaccard score, so similarity values mean the same thing as under exact search. Recall is not guaranteed - a small number of genuine near-duplicates may be missed, which makes these counts a lower bound.

Rerun `audit.py` to reproduce every number above.
