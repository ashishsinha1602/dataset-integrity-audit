# BIRD-CRITIC 1.0 (open) - integrity report

Generated by `audit.py prep` from `bird-critic-open.jsonl`.

## Dataset shape

- **600** records
- **15** distinct databases (40.0 items per database on average)
- largest single database accounts for **13.8%** of all records
- **30** records flagged `efficiency=true`

| Dialect | Records |
|---|---|
| PostgreSQL | 300 |
| MySQL | 100 |
| Oracle | 100 |
| SQLServer | 100 |

| Category | Records |
|---|---|
| Query | 304 |
| Personalization | 162 |
| Management | 104 |
| Efficiency | 30 |

## Integrity findings

- malformed JSON lines: **0**
- schema violations: **0**
- degenerate records (empty query / issue_sql / db_id): **0**
- exact duplicate `query` groups: **4** covering 8 records (1.3%)
- exact duplicate `issue_sql` groups: **5** covering 10 records (1.7%)
- near-duplicate pairs at Jaccard >= 0.7: **4**, of which **0** span two different SQL dialects
- near-duplicate clusters: **4**, containing 8 records (1.3%)
  - of these, **1** are identical across every evaluated field (2 records, 0.3%) and are genuinely redundant
  - the remaining **3** share a question stem but differ in `issue_sql` or `category`, which is a deliberate construction rather than a defect

## How to read this

The headline is that this benchmark is structurally sound. Schema validation passes on every record, nothing is degenerate, and the duplicate rate is low enough to be immaterial to a reported score.

The distinction between an identical cluster and a shared-stem variant is the part worth keeping. A naive text-similarity pass flags both, which would overstate the problem; only the identical clusters let a model bank the same answer twice.

The one number worth carrying into any claim about generalisation is database reuse: 600 records are drawn from just 15 databases, averaging 40.0 items each. That is a property of the benchmark's design, not an error, but it does mean schema-specific familiarity is rewarded.

Every number above is computed by `audit.py`; rerun it to reproduce.
